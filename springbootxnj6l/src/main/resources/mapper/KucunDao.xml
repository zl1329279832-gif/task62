<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.KucunDao">

    <!-- 悲观锁查询物资行 -->
    <select id="selectForUpdate" resultType="com.entity.WuzixinxiEntity">
        SELECT * FROM wuzixinxi WHERE wuzimingcheng = #{wuzimingcheng} FOR UPDATE
    </select>

    <!-- SQL 层原子增加库存 -->
    <update id="increaseStockDirect">
        UPDATE wuzixinxi
        SET wuzishuliang = wuzishuliang + #{delta}
        WHERE wuzimingcheng = #{wuzimingcheng}
    </update>

    <!-- 按物资分类聚合低库存预警 -->
    <select id="selectLowStockAlert" resultType="map">
        SELECT
            wuzifenlei,
            COUNT(*) AS lowStockCount,
            GROUP_CONCAT(wuzimingcheng SEPARATOR ',') AS wuziNames,
            MIN(wuzishuliang) AS minQuantity
        FROM wuzixinxi
        WHERE wuzishuliang &lt;= #{threshold}
        GROUP BY wuzifenlei
    </select>

</mapper>
