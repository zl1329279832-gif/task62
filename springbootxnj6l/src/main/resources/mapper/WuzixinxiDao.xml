<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.WuzixinxiDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.WuzixinxiEntity" id="wuzixinxiMap">
        <result property="wuzimingcheng" column="wuzimingcheng"/>
        <result property="wuzifenlei" column="wuzifenlei"/>
        <result property="wuzitupian" column="wuzitupian"/>
        <result property="wuziguige" column="wuziguige"/>
        <result property="wuzipinpai" column="wuzipinpai"/>
        <result property="wuzishuliang" column="wuzishuliang"/>
        <result property="wuzixiangqing" column="wuzixiangqing"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.WuzixinxiVO" >
		SELECT * FROM wuzixinxi  wuzixinxi         
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.WuzixinxiVO" >
		SELECT  wuzixinxi.* FROM wuzixinxi  wuzixinxi 	
 		<where> 1=1 ${ew.sqlSegment}</where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.WuzixinxiView" >

		SELECT  wuzixinxi.* FROM wuzixinxi  wuzixinxi 	        
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.WuzixinxiView" >
		SELECT * FROM wuzixinxi  wuzixinxi <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	

    <select id="selectValue" resultType="map" >
        SELECT ${params.xColumn}, sum(${params.yColumn}) total FROM wuzixinxi
        <where> 1=1 ${ew.sqlSegment}</where>
        group by ${params.xColumn}
    </select>

    <select id="selectTimeStatValue" resultType="map" >
        <if test = 'params.timeStatType == "日"'>
                SELECT DATE_FORMAT(${params.xColumn},'%Y-%m-%d') ${params.xColumn}, sum(${params.yColumn}) total FROM wuzixinxi
                <where> 1=1 ${ew.sqlSegment}</where>
                group by DATE_FORMAT(${params.xColumn},'%Y-%m-%d')
        </if>
        <if test = 'params.timeStatType == "月"'>
                SELECT DATE_FORMAT(${params.xColumn},'%Y-%m') ${params.xColumn}, sum(${params.yColumn}) total FROM wuzixinxi
                <where> 1=1 ${ew.sqlSegment}</where>
                group by DATE_FORMAT(${params.xColumn},'%Y-%m')
        </if>
        <if test = 'params.timeStatType == "年"'>
                SELECT DATE_FORMAT(${params.xColumn},'%Y') ${params.xColumn}, sum(${params.yColumn}) total FROM wuzixinxi
                <where> 1=1 ${ew.sqlSegment}</where>
                group by DATE_FORMAT(${params.xColumn},'%Y')
        </if>
    </select>

    <select id="selectGroup" resultType="map" >
        SELECT ${params.column} , count(1) total FROM wuzixinxi
        <where> 1=1 ${ew.sqlSegment}</where>
        group by ${params.column}
    </select>

    <!-- 按物资分类聚合低库存预警 -->
    <select id="selectLowStockAlert" resultType="map">
        SELECT
            wuzifenlei,
            COUNT(*) AS lowStockCount,
            GROUP_CONCAT(wuzimingcheng SEPARATOR ',') AS wuziNames,
            MIN(wuzishuliang) AS minQuantity
        FROM wuzixinxi
        WHERE wuzishuliang &lt;= #{threshold}
        GROUP BY wuzifenlei
    </select>
</mapper>
